#include "main.h"

/**
 * _islower - checks for lowercase character
 * Return 1 if character is lowercase and 0 for other
 */
int _islower(int c)
{
	int c;

	for (c = 97 && c <= 122)
	{
		return (1);
	}
	return (0);
}
